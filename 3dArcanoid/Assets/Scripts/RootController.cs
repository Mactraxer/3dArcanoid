using UnityEngine;

public class RootController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameObject brickController = GameObject.FindGameObjectWithTag("BrickManager");
        
    }

}
